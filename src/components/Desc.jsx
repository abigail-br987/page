
function Desc({ title, content }) {
    return (
        <section id="block">
            <div>
                <h1>{title}</h1>
                <p>{content}</p>
            </div>
        </section>
    );
}

export default Desc;